class Array <type T = any> {

  final type elementType = T;
  T[] .innerValues;

  Array(T... values) {
    self.innerValues = Arrays.copy(values);
  }

  T[] _add_(any[] values) {
    T[] ret = T[self.innerValues.length];
    if (values.length != self.innerValues.length)
      throw ArithmeticException("Array lengths are not equal (%d != %d)." % [values.length, self.innerValues.length]);
    for ()
  }

}